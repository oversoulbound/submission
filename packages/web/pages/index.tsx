import type { NextPage } from "next";
import SketchComponent from "../components/SketchComponent";

const Home: NextPage = () => {
  return (
    <div>
      <SketchComponent />
    </div>
  );
};

export default Home;
