{
  "name": "oversoulbound",
  "version": "0.0.1",
  "license": "Unlicense",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "dev": "run-p dev:*",
    "predev": "yarn workspace @oversoulbound/functions build",
    "dev:web": "yarn workspace @oversoulbound/web dev",
    "dev:functions": "yarn workspace @oversoulbound/functions dev",
    "dev:firebase": "firebase emulators:start --only functions,firestore,pubsub"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "firebase-tools": "^11.2.0",
    "npm-run-all": "^4.1.5"
  },
  "private": true
}
