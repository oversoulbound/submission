{
  "name": "oversoulbound",
  "version": "0.0.1",
  "license": "Unlicense",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "ci": "yarn lint && yarn build",
    "lint": "run-p lint:*",
    "lint:contracts": "yarn workspace @oversoulbound/contracts lint",
    "lint:functions": "yarn workspace @oversoulbound/functions lint",
    "lint:web": "yarn workspace @oversoulbound/web lint",
    "fix": "run-p fix:*",
    "fix:contracts": "yarn workspace @oversoulbound/contracts fix",
    "fix:functions": "yarn workspace @oversoulbound/functions fix",
    "fix:web": "yarn workspace @oversoulbound/web fix",
    "predev": "yarn workspace @oversoulbound/functions build",
    "dev": "run-p dev:*",
    "dev:functions": "yarn workspace @oversoulbound/functions dev",
    "dev:web": "yarn workspace @oversoulbound/web dev",
    "dev:firebase": "firebase emulators:start --only functions,firestore,pubsub",
    "build": "run-p build:*",
    "build:contracts": "yarn workspace @oversoulbound/contracts build",
    "build:functions": "yarn workspace @oversoulbound/functions build",
    "build:web": "yarn workspace @oversoulbound/web build"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "firebase-tools": "^11.2.0",
    "npm-run-all": "^4.1.5"
  },
  "private": true
}
